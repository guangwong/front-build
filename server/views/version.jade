extends layout

block content
  .container
    .row-fluid
      .span12
        .fb-title
          h1 
            a(href='/app?root=#{app.rootDir}')= app.name
            |/ 
            span= page.name
            |/
            sapn= page.version
          p.path= page.rootDir

          .buildto.form-inline
            .input-prepend
              |&nbsp;
              span.add-on
                i.icon-calendar
              input.timestamp-input(type='text')
              |&nbsp;
              a.btn.fb-build-page(href='/build-page/#{page.name}@#{page.version}?root=#{app.rootDir}')
                |Build            
              span.status

        #reports
        ul.timestamps
          - each ts in timestamps
            li= ts
  script
    KISSY.use('page/index');